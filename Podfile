platform :ios, '11.0'
use_frameworks!
inhibit_all_warnings!

target 'SimpleRSSReader' do

    pod 'FeedKit'
    pod 'DTTableViewManager'
    pod 'PKHUD', '~> 5.0'

    target 'SimpleRSSReaderTests' do
        inherit! :search_paths
    end
end
